#include<iostream>
using namespace std;

{
	int n;
	cin >> n;
	int mod N = 0;
	for (int i = 2; i < (a / 2); i++) {
		if (n%i == 0) {
			mod N++;
		}
	}
	if (modN == 0) {
		cout << "primary";
	}
	return 0;
}